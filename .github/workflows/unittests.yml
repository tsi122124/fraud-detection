name: Unit Tests — Fraud Detection

on:
  push:
    branches: [main, task-1, task-2, task-3]
  pull_request:
    branches: [main, task-1, task-2, task-3]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Cache pip dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov
          pip install pandas numpy scikit-learn imbalanced-learn xgboost lightgbm

      - name: Lint with flake8
        run: |
          pip install flake8
          # Fail on real errors only (syntax errors, undefined names, unused imports)
          flake8 src/ tests/ --count --select=E9,F63,F7,F82 --show-source --statistics
          # Report all issues using config but never fail build
          flake8 src/ tests/ --count --statistics --exit-zero

      - name: Run unit tests with coverage
        run: |
          pytest tests/ -v --tb=short \
            --cov=src \
            --cov-report=term-missing \
            --cov-report=xml

      - name: Upload coverage report
        uses: actions/upload-artifact@v3
        with:
          name: coverage-report
          path: coverage.xml
